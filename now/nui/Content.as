package now.nui {

	/**
	 * 一个空的UI组件，用来作为Container容器的中间层
	 */
	public class Content extends Ui {
		/**
		 * @inheritDoc
		 */
		public function Content(xpos:Number = 0, ypos:Number = 0){
			super(xpos, ypos);
		}
	}
}